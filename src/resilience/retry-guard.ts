// Thin re-export of @olivierzal/api-core (the mechanism formerly
// duplicated here as melcloud-api's twin). The core kept this repo's
// monotonic deadline and added melcloud-api's `Disposable` surface.
export { RetryGuard } from '@olivierzal/api-core'
