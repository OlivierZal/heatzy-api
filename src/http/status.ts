// Thin re-export of @olivierzal/api-core's status table (the union of
// both consumers' needs — `NotFound` and `TooManyRequests` arrived
// with melcloud-api's vocabulary and are additive here).
export { HttpStatus } from '@olivierzal/api-core'
