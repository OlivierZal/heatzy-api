export enum Mode {
  cft = 0,
  cft1 = 4,
  cft2 = 5,
  eco = 1,
  fro = 2,
  stop = 3,
}

export enum DerogMode {
  boost = 2,
  off = 0,
  vacation = 1,
}

export enum Switch {
  off = 0,
  on = 1,
}
