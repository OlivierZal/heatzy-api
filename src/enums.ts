export enum DerogMode {
  boost = 2,
  off = 0,
  vacation = 1,
}

export enum Mode {
  cft = 0,
  eco = 1,
  fro = 2,
  stop = 3,
  // Product: not 'v1' or 'v2'
  cft1 = 4,
  cft2 = 5,
}

export enum Switch {
  off = 0,
  on = 1,
}

export enum TemperatureCompensation {
  minus5C = 0,
  noChange = 50,
  plus5C = 100,
}
