# Calls the family's reusable publish (pack, attest, publish to GitHub
# Packages): the caller keeps the `release` trigger and grants exactly
# what the called job declares; the `npm` environment travels with it.
jobs:
  publish:
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    uses: OlivierZal/configs/.github/workflows/reusable-publish.yml@76559db426286aab1f8e5ae8b0f98346cd9dfd2b # v6.0.0
name: Publish package to GitHub Packages
on:
  release:
    types: [published]
permissions: {}
